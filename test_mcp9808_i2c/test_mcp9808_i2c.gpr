with "config/test_mcp9808_i2c_config.gpr";

with "rp2040_hal";
with "pico_bsp.gpr";

with "mcp9808_i2c";

project Test_Mcp9808_I2c is

   for Target use "arm-eabi";
   for Runtime ("Ada") use "light-cortex-m0p";

   for Source_Dirs use ("src/", "config/");
   for Object_Dir use "obj/" & Test_Mcp9808_I2c_Config.Build_Profile;
   for Create_Missing_Dirs use "True";
   for Exec_Dir use "bin";
   for Main use ("test_mcp9808_i2c.adb");

   package Compiler is
      for Default_Switches ("Ada") use Test_Mcp9808_I2c_Config.Ada_Compiler_Switches;
   end Compiler;

   package Binder is
      for Switches ("Ada") use ("-Es"); --  Symbolic traceback
   end Binder;

   package Install is
      for Artifacts (".") use ("share");
   end Install;

   package Linker is
      for Switches ("Ada") use RP2040_HAL.Linker_Switches &
         ("-Wl,-print-memory-usage");
   end Linker;

end Test_Mcp9808_I2c;
